# laravel-admin-base

> A Laravel project

## Build Setup

``` bash
# install dependencies
composer install

# publish resources
php artisan chen:publish

# install production
php artisan chen:install

```
